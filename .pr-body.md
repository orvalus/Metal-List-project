## Summary
Comprehensive test infrastructure for backend and frontend:
- Issue #12: Set up pytest + vitest frameworks with coverage tools
- Issue #16: Configure coverage reporting with 95% targets
- Issue #23: Infrastructure + models upgrade for integration tests

## Changes
**Backend:**
- Added pytest.ini + .coveragerc with coverage thresholds
- Added pytest-cov 4.1.0 to requirements.txt
- Upgraded models.py for SQLAlchemy 2.0 (Mapped[] type hints)
- Added conftest.py with in-memory test DB fixtures
- 36 tests passing (setup + config + existing)

**Frontend:**
- Added vitest + React Testing Library to devDependencies
- Added vitest.config.js with coverage provider (v8)
- Added setupTests.js for RTL initialization
- Added test scripts to package.json (test, test:coverage)
- 7 tests passing (setup + App rendering)

**CI/CD:**
- Added .github/workflows/test-coverage.yml (GitHub Actions)
- Codecov integration ready

**Documentation:**
- Added COVERAGE_GOALS.md with targets, workflows, and metrics
- Implementation plan for Issue #23 (ready for full test suite)

## Test Results
- Backend: 36/36 tests passing
- Frontend: 7/7 tests passing
- All existing tests still pass

## Files Changed
- backend/requirements.txt (pytest-cov)
- backend/pytest.ini (NEW)
- backend/.coveragerc (NEW)  
- backend/models.py (SQLAlchemy 2.0 upgrade)
- backend/conftest.py (NEW - test fixtures)
- backend/test_setup.py (9 tests)
- backend/test_coverage_config.py (8 tests)
- frontend/package.json (test tools)
- frontend/vitest.config.js (NEW)
- frontend/src/setupTests.js (NEW)
- frontend/src/App.test.jsx (2 tests)
- frontend/src/coverage-config.test.js (5 tests)
- .github/workflows/test-coverage.yml (NEW)
- COVERAGE_GOALS.md (NEW)
- .gitignore (added coverage folders)

Closes #12, #16
Related to #23 (infrastructure ready, full test suite pending)
